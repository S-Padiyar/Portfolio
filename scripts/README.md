# Scripts

These are legacy one-off refactor helpers from earlier project cleanup passes.

They are not part of the normal development workflow and are not called by
`package.json`. Keep new routine automation in `package.json` scripts or document
it here with its purpose, inputs, and expected output.
